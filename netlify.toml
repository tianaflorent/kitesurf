[build]
  command = "npm run build"
  publish = ".next"

# Le plugin Next.js est auto-installé par Netlify,
# mais on le déclare explicitement pour la clarté.
[[plugins]]
  package = "@netlify/plugin-nextjs"
